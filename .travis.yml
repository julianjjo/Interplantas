language: ruby
rvm:
 - 2.1.2p95
before_script:
 - mysql -e 'create database Interplantas_test;'
install: 
 - gem install bundler
 - bundle config build.nokogiri --use-system-libraries
 - bundle install
script:
 - rake cucumber 